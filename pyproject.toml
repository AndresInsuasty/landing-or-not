[project]
name = "aterrizaje"
version = "0.1.0"
description = "Simulador de aterrizaje lunar para clase de algoritmos — herramienta educativa interactiva con pygame"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "matplotlib>=3.10.9",
    "pygame>=2.6.1",
]
